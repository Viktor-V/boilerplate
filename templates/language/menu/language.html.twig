{% set languages = languages() %}

{% if languages|length > 1 %}
        <li class="menu-language-item">
            {% for code, locale in languages %}
                <a class="menu-link" href="{{ languagePath(app.request, code) }}">{{ code }}</a>
            {% endfor %}
        </li>
{% endif %}
