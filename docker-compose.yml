version: "3.4"

services:
    web:
        container_name: web
        build:
            context: .
            target: dev
        volumes:
            - ./:/var/www/html
        ports:
            - 80:80
